# Example values only. Do not commit real credentials.
TZ=Asia/Seoul
MARIADB_ROOT_PASSWORD=replace-in-local-env-only
PORT=8080
TRUST_PROXY=0
API_KEY=replace-in-local-env-only
DB_HOST=mariadb
DB_PORT=3306
DB_USER=backend_admin
DB_PASSWORD=replace-in-local-env-only
DB_DATABASE=ingest_backend_db
DB_CONN_LIMIT=10
TASK_NAME=SessionFlow
WORKFLOW_IDLE_MS=120000
API_PATH_MAX=1024
IDENTITY_HMAC_SECRET=replace-in-local-env-only
