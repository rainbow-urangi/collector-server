name: collector-server-cd

on:
  workflow_run:
    workflows:
      - collector-server-ci
    types:
      - completed

jobs:
  note:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'dev' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "dev deploy is handled by the test server systemd timer"