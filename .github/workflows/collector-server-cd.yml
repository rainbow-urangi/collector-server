# name: collector-server-cd

# on:
#   workflow_run:
#     workflows:
#       - collector-server-ci
#     types:
#       - completed

# jobs:
#   publish-image:
#     if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main' }}
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout target revision
#         uses: actions/checkout@v6
#         with:
#           ref: ${{ github.event.workflow_run.head_sha }}
        
#       - name: Prepare image metadata
#         run: |
#           OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
#           SHORT_SHA=$(echo "${{ github.event.workflow_run.head_sha }}" | cut -c1-7)
#           echo "IMAGE_URI=ghcr.io/${OWNER_LC}/collector-server" >> "$GITHUB_ENV"
#           echo "IMAGE_TAG_SHA=sha-${SHORT_SHA}" >> "$GITHUB_ENV"

#       - name: Log in to GHCR
#         run: |
#           echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

#       - name: Build image
#         run: |
#           docker build \
#             -t "${IMAGE_URI}:latest" \
#             -t "${IMAGE_URI}:${IMAGE_TAG_SHA}" \
#             .

#       - name: Push image
#         run: |
#           docker push "${IMAGE_URI}:latest"
#           docker push "${IMAGE_URI}:${IMAGE_TAG_SHA}"

#       - name: Output published tags
#         run: |
#           echo "Published: ${IMAGE_URI}:latest"
#           echo "Published: ${IMAGE_URI}:${IMAGE_TAG_SHA}"